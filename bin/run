#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

if [ ! -f .env ]; then
  echo ".env missing. Run bin/setup first." >&2
  exit 1
fi

cd analysis-app
bundle exec rails db:prepare
bundle exec rails server "$@"
