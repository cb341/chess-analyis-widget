source "https://rubygems.org"

# Rails is intentionally not declared for this dependency-free POC because the
# current environment has no Rails and network installs are out of scope.
# Add Rails here when converting the skeleton into a full app.
