# frozen_string_literal: true

# Placeholder for the Rails model described in ../app_spec.md.
# In dependency-free POC mode analyses are rendered inline and not persisted.
# Represents a persisted chess analysis once the skeleton is converted to Rails.
class Analysis
end
